# Scheduler config file
# Comments begin with '#'
# Each line is either a setting, a comment, or empty
# Duplicates are overwritten by bottom-most values

# Trace File
trace				traces/Thunder.trace
nodes				1024
cpus_per_node	4
nodes_per_switch	8

# use -1 for all
max_job_count	100

# Scheduler: backfill, fcfs
schedule			fcfs

# Node allocation: first-available, largest-contiguous
allocate			largest-contiguous

#Output file names
vis_input		outputs/Thunder-fcfs-LC.csv
